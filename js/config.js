const Config = {
    // URL for the backend server
    // Change this to your deployed cloud URL once deployed
    API_BASE_URL: 'https://sunshine-backend-w14m.onrender.com'
};

window.Config = Config;
